#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../js-yaml/bin/js-yaml.js" "$@"
else
  exec node  "$basedir/../../../../js-yaml/bin/js-yaml.js" "$@"
fi
# cmd-shim-target=/Users/justin_devsecops/Docusaurus-Lern-Tagebuch/node_modules/.pnpm/@docusaurus+utils-validation@3.5.2_@docusaurus+types@3.5.2_react-dom@18.3.1_react@18.3.1__react@18.3.1__typescript@5.5.4/node_modules/js-yaml/bin/js-yaml.js
