#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../jiti/bin/jiti.js" "$@"
else
  exec node  "$basedir/../../../jiti/bin/jiti.js" "$@"
fi
# cmd-shim-target=/Users/justin_devsecops/Docusaurus-Lern-Tagebuch/node_modules/.pnpm/postcss-loader@7.3.4_postcss@8.4.45_typescript@5.5.4_webpack@5.94.0/node_modules/jiti/bin/jiti.js
