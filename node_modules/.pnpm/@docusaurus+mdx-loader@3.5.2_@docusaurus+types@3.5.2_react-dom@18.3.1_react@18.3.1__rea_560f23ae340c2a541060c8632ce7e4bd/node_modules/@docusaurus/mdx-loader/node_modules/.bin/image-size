#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../image-size/bin/image-size.js" "$@"
else
  exec node  "$basedir/../../../../image-size/bin/image-size.js" "$@"
fi
# cmd-shim-target=/Users/justin_devsecops/Docusaurus-Lern-Tagebuch/node_modules/.pnpm/@docusaurus+mdx-loader@3.5.2_@docusaurus+types@3.5.2_react-dom@18.3.1_react@18.3.1__rea_560f23ae340c2a541060c8632ce7e4bd/node_modules/image-size/bin/image-size.js
