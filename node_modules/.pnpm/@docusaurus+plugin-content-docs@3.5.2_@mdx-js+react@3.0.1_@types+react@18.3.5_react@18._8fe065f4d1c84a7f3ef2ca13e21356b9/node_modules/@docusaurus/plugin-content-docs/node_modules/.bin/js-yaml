#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../js-yaml/bin/js-yaml.js" "$@"
else
  exec node  "$basedir/../../../../js-yaml/bin/js-yaml.js" "$@"
fi
# cmd-shim-target=/Users/justin_devsecops/Docusaurus-Lern-Tagebuch/node_modules/.pnpm/@docusaurus+plugin-content-docs@3.5.2_@mdx-js+react@3.0.1_@types+react@18.3.5_react@18._8fe065f4d1c84a7f3ef2ca13e21356b9/node_modules/js-yaml/bin/js-yaml.js
