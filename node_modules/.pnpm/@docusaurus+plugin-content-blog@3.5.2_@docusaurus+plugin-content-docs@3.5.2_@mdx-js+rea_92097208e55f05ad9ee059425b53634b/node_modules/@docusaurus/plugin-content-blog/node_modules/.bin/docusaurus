#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../core/bin/docusaurus.mjs" "$@"
else
  exec node  "$basedir/../../../core/bin/docusaurus.mjs" "$@"
fi
# cmd-shim-target=/Users/justin_devsecops/Docusaurus-Lern-Tagebuch/node_modules/.pnpm/@docusaurus+plugin-content-blog@3.5.2_@docusaurus+plugin-content-docs@3.5.2_@mdx-js+rea_92097208e55f05ad9ee059425b53634b/node_modules/@docusaurus/core/bin/docusaurus.mjs
